---
// The "Now" section is edited alongside the rest of the About page now. This
// route stays as a redirect so bookmarks to the old screen keep working.
return Astro.redirect('/admin/about', 301);
---
